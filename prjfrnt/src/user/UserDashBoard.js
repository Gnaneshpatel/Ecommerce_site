import React from 'react';
import Base from '../core/Base';

const UserDashBoard = () => {
    return (
        <Base>
            <h1 className="text-center text-muted">UserDashBoard</h1>
        </Base>
    );
}

export default UserDashBoard;
