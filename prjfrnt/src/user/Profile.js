import React from 'react';
import Base from '../core/Base';

const Profile = () => {
    return (
        <Base>
            <h1 className="text-center text-muted">User Profile</h1>
        </Base>
    );
}

export default Profile;
